# Alternative minimal requirements for environments with build issues
# Use this if the main requirements.txt fails to build

fastapi==0.88.0
uvicorn==0.20.0
python-dotenv==0.21.0
google-generativeai==0.3.0
pydantic==1.10.8
requests==2.28.2
